let arr=[1, 2, 3, 4];
let arr2=arr;

arr2.pop()

// Explanation:
// Creation:

// let arr = [1, 2, 3, 4];: This creates an array arr with values [1, 2, 3, 4].

// let arr2 = arr;: This assigns arr2 the same reference as arr. Both arr and arr2 now point to the same array in memory.

// Modification:

// arr2.pop();: This removes the last element from the array that both arr and arr2 reference.

let a; // In here, Variable 'a' implicitly has an 'any' type.

let variable:string; // Here we defied type of variable


// tuple
let arr1:[number, string]=[1, "hey"] 
let arr3:[boolean, number, string];
arr3=[false,121, "Hello" ];

console.log(arr3)





